# GitHub bot principals allowed through the trusted-bot exception path.
# This file is consulted when a commit is authored by a recognised GitHub
# App/Bot and cannot be verified through local GPG/SSH or the GitHub
# commit-verification API.
#
# Each non-empty, non-comment line is matched exactly against the
# `author.login` field returned by the GitHub Commits API.
Codex
Copilot
dependabot[bot]
github-actions[bot]
