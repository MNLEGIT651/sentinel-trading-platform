{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- apps/web packages/shared",
  "installCommand": "corepack enable pnpm && pnpm install",
  "buildCommand": "pnpm --filter @sentinel/web run build",
  "env": {
    "PNPM_HOME": ".pnpm"
  }
}
