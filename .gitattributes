# Normalize line endings to LF on commit, auto-detect on checkout
* text=auto eol=lf

# Force binary for known binary types
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary

# Lock files – treat as generated (excluded from GitHub language stats)
pnpm-lock.yaml    linguist-generated
